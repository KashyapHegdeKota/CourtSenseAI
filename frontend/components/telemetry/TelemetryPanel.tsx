"use client";
import { Activity, Gauge, Ruler, Zap } from "lucide-react";
import { Area, AreaChart, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts";
import type { PlayerTelemetry, Telemetry } from "../../lib/types";
type Props={telemetry?:Telemetry};
const fallback:PlayerTelemetry[]=Array.from({length:14},(_,i)=>({id:i+1,team:i<7?"A":"B",x:0,y:0,speed_kmh:8+(i*7)%24,distance_m:4200+i*183}));
export function TelemetryPanel({telemetry}:Props){const p=telemetry?.players?.length?telemetry.players:fallback;const top=Math.max(...p.map(x=>x.speed_kmh));const sprints=p.filter(x=>x.speed_kmh>24).length;const chart=p.slice(0,8).map((x,i)=>({name:`P${x.id}`,speed:x.speed_kmh,distance:Math.round(x.distance_m/100)}));return <section className="grid gap-4 lg:grid-cols-[1.1fr_1fr]"><div className="panel p-5"><div className="mb-4 flex items-center justify-between"><div><p className="eyebrow">Kinematics</p><h3>Player output</h3></div><Activity size={16} className="text-emerald-300"/></div><div className="grid grid-cols-3 gap-2"><div className="metric"><Zap size={15} className="text-orange-300"/><p className="eyebrow">Top speed</p><strong>{top.toFixed(1)} <small>km/h</small></strong></div><div className="metric"><Gauge size={15} className="text-emerald-300"/><p className="eyebrow">Sprints</p><strong>{sprints} <small>&gt;24 km/h</small></strong></div><div className="metric"><Ruler size={15} className="text-cyan-300"/><p className="eyebrow">Compactness</p><strong>1,842 <small>m²</small></strong></div></div><div className="mt-5 h-40"><ResponsiveContainer width="100%" height="100%"><AreaChart data={chart}><defs><linearGradient id="speedFill" x1="0" y1="0" x2="0" y2="1"><stop offset="0" stopColor="#34d399" stopOpacity={.35}/><stop offset="1" stopColor="#34d399" stopOpacity={0}/></linearGradient></defs><XAxis dataKey="name" hide/><YAxis hide domain={[0,35]}/><Tooltip contentStyle={{background:"#0b1c22",border:"1px solid #24414a",fontSize:11}}/><Area type="monotone" dataKey="speed" stroke="#34d399" fill="url(#speedFill)" strokeWidth={2}/></AreaChart></ResponsiveContainer></div></div><div className="panel p-5"><p className="eyebrow">Distance covered</p><h3 className="mb-5">Squad workload</h3><div className="space-y-3">{p.slice(0,7).map(x=><div key={String(x.id)}><div className="mb-1 flex justify-between text-xs"><span>Player {x.id}</span><span className="text-slate-400">{Math.round(x.distance_m).toLocaleString()} m</span></div><div className="h-1.5 rounded-full bg-white/10"><div className="h-full rounded-full bg-gradient-to-r from-cyan-400 to-emerald-300" style={{width:`${Math.min(100,x.distance_m/65)}%`}}/></div></div>)}</div></div></section>}
